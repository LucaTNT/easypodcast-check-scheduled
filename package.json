{
  "private": true,
  "name": "easypodcast-check-scheduled",
  "version": "1.0.0",
  "description": "A worker that checks if a new episode of EasyApple has been scheduled and notifies Federico otherwise.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "dev": "npx wrangler dev"
  },
  "author": "Luca Zorzi <luca@lucazorzi.net>",
  "license": "MIT",
  "devDependencies": {
    "miniflare": "^4.20251202.1",
    "prettier": "3.7.4"
  }
}
